import Link from 'next/link'; import {categories} from '@/lib/game-data';
export default function Rules(){const groups=Object.groupBy(categories,(c)=>c.group);return <main className="inner-page"><header className="site-header"><Link className="brand" href="/"><span className="brand-mark">51</span><span><strong>Fantasy Survivor</strong><small>League rulebook</small></span></Link><nav><Link href="/">Standings</Link><Link href="/draft">Draft board</Link><Link className="active" href="/rules">Rules</Link><Link className="admin-link" href="/admin">Game master</Link></nav></header><section className="inner-hero rules-hero"><p className="eyebrow"><span/> Official scoring</p><h1>Every move has a price.</h1><p>The same scoring system as the workbook, including the round-three blind-pick multiplier.</p></section><section className="rules-layout"><aside><div className="rule-summary"><span>$20</span><strong>Entry fee</strong><p>League entry for the new season.</p></div><div className="rule-summary"><span>3</span><strong>Castaways drafted</strong><p>Three rounds, with round three eligible for the blind-pick bonus.</p></div><div className="rule-summary"><span>1.25×</span><strong>Blind-pick bonus</strong><p>Keep the blind pick and that castaway’s score receives a 25% boost.</p></div></aside><div className="rules-groups">{Object.entries(groups).map(([group,items])=><article key={group}><h2>{group}</h2><div>{items?.map((c)=><div className="scoring-rule" key={c.id}><span className={c.points<0?'negative':''}>{c.points>0?'+':''}{c.points}</span><p><strong>{c.label}</strong></p></div>)}</div></article>)}</div></section></main>}
