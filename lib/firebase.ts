export const firebaseConfigured = Boolean(process.env.NEXT_PUBLIC_FIREBASE_API_KEY && process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID);
export const firebaseConfig = { apiKey:process.env.NEXT_PUBLIC_FIREBASE_API_KEY, authDomain:process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN, projectId:process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID, storageBucket:process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET, messagingSenderId:process.env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID, appId:process.env.NEXT_PUBLIC_FIREBASE_APP_ID };
export type FirebaseUser = { email:string|null; displayName?:string|null };
type CompatFirebase = { apps:unknown[]; initializeApp:(config:typeof firebaseConfig)=>void; auth:(()=>{onAuthStateChanged:(cb:(user:FirebaseUser|null)=>void)=>()=>void;signInWithPopup:(provider:unknown)=>Promise<unknown>;signOut:()=>Promise<void>})&{GoogleAuthProvider:new()=>unknown}; firestore:()=>{collection:(name:string)=>{doc:(id:string)=>{onSnapshot:(ok:(snapshot:{exists:boolean;data:()=>unknown})=>void,bad:()=>void)=>()=>void;set:(data:unknown)=>Promise<void>}}} };
declare global { interface Window { firebase?:CompatFirebase } }
export async function getFirebase():Promise<CompatFirebase|null>{if(!firebaseConfigured||typeof window==='undefined')return null;for(let tries=0;tries<100&&!window.firebase;tries++)await new Promise((resolve)=>setTimeout(resolve,25));const firebase=window.firebase;if(!firebase)return null;if(!firebase.apps.length)firebase.initializeApp(firebaseConfig);return firebase}
